---
type: ratification_log
---

# Ratification Log

Append-only. This is how drafts propose settled positions without silently
becoming them. A node referenced as Accepted here is what makes
`scribectl status` report it ratified.

## <date>

### Accepted
- "<claim>" introduced in `<draft>` → promoted to [[Node]] — <reason>

### Rejected
- "<claim>" (source `<draft>`) — <reason it conflicts>

### Deferred
- "<claim>" (source `<draft>`) — <what it depends on>
