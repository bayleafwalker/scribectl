---
type: timeline
---

# Timeline

Append-only. Optional for this set — essay cards carry no position, so every
pack sees everything recorded here. Use it (if at all) for milestones the
essays must stay consistent about: what shipped when, what was decided when.
One bullet per event under `## pre`. Never edit or reorder existing bullets.
Format: `- actors: A, B | loc: Place | fact`
(Any bullet under a position heading is a real event and ships into packs —
which is why this template seeds no example bullet.)

## pre
