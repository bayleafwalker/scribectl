---
type: ratification_log
---

# Ratification Log

Append-only. This is how outputs propose canon without silently becoming it —
and how design rulings become mechanics canon. A node referenced as Accepted
here is what makes `scribectl status` report it ratified; lore and mechanics
ratify through the same ledger because they rot the same way.

## <date>

### Accepted
- "<thing>" introduced in `<draft or design note>` → promoted to [[Node]] — <reason>

### Rejected
- "<thing>" (source `<draft>`) — <reason it conflicts>

### Deferred
- "<thing>" (source `<draft>`) — <what it depends on>
