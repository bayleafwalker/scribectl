---
type: ratification_log
---

# Ratification Log

Append-only. This is how prose proposes canon without silently becoming it.
A node referenced as Accepted here is what makes `ff status` report it ratified.

## <date>

### Accepted
- "<thing>" introduced in `<draft>` → promoted to [[Node]] — <reason>

### Rejected
- "<thing>" (source `<draft>`) — <reason it conflicts>

### Deferred
- "<thing>" (source `<draft>`) — <what it depends on>
